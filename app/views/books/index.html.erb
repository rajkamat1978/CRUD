<% @books.each do |book| %>	
<%= book.title %>
<%= book.desc %>

<%= link_to 'Delete', book_path(book), 
                             method: :delete, 
                             data: { confirm: 'Are you sure?' } %> 
<% end -%>